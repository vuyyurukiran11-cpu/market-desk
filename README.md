# Market Desk repositories

This parent repository groups two independent repositories:

- [`market/`](market) — the Market Desk application source code
- [`e2e_tests/`](e2e_tests) — Playwright + TypeScript regression tests

The E2E project checks out the `market` repository separately in CI.
