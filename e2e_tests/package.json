{
  "name": "market-e2e-tests",
  "private": true,
  "scripts": { "test": "playwright test", "install:browsers": "playwright install chromium" },
  "devDependencies": { "@playwright/test": "^1.55.0", "typescript": "^5.9.0" }
}
