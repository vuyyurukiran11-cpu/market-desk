# Market Desk E2E tests

Standalone Playwright + TypeScript regression tests for the `market` app repo.

```sh
npm install
npm run install:browsers
npm test
```

By default, tests expect the app checkout at `../market`. Set `APP_DIR` to use another local checkout.
